/* eslint-env es6 */
/**
 * @type {import('prettier').Options}
 */
const config = {
    printWidth: 120,
    trailingComma: "all",
    singleQuote: false,
    endOfLine: "lf",
    arrowParens: "avoid",
};

module.exports = config;